<div class="layout-row layout-xs-column">
  <div class="layout-column flex-xs flex-gt-xs-50">
    {{#paper-card}}
      <TestTable></TestTable>
      {{#each model as |user|}}
        <li>{{user.name}}</li>
      {{/each}}
    {{/paper-card}}
  </div>
</div>

{{outlet}}